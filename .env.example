# ── Multi-Provider API Keys ──────────────────────────────────
# Add multiple keys per provider, comma-separated
GROQ_API_KEYS=
GEMINI_API_KEYS=
HF_API_KEYS=
# Legacy single key (fallback if no multi-keys set)
# ANTHROPIC_API_KEY=sk-ant-xxx

# ── MongoDB ──────────────────────────────────────────────────
# MONGO_URI=mongodb://localhost:27017
